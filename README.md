Tank meter for SignalK, running on an esp8266 with (optional, easy to remove) OLED display.

Uses a set of capacitive / ultrasonic sensors to measure the height of the fluid, combines the readings
and sends it as a ratio 0-1 to SignalK

